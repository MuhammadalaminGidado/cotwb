ALTER TABLE "pieces" ADD COLUMN "search_vector" "tsvector" GENERATED ALWAYS AS (setweight(to_tsvector('english', title), 'A') || setweight(to_tsvector('english', body), 'B')) STORED;--> statement-breakpoint
CREATE INDEX "pieces_search_vector_idx" ON "pieces" USING gin ("search_vector");